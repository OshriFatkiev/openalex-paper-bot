"""Expose package metadata for the OpenAlex paper bot.

This module keeps the package version in one place so both packaging metadata
and runtime code can reference it consistently.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
